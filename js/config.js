// External Configuration
// This file is loaded before app.js and overrides the default settings.
// Useful for specific machine settings when running via file:// protocol.

window.RGMX_CONFIG = {
    "location_id": "dev",
    "api_url": "###https://vps1.therad.co.za/rgmx/upload.php"
};
